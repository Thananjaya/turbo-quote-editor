<main class="container">
  <%= turbo_frame_tag "quote_first_frame" do %>
    <div class="header">
      <h1>Quotes</h1>
      <%= link_to "New quote", new_quote_path, data: {turbo_frame: dom_id(Quote.new)}, class: "btn btn--primary" %>
    </div>
  <% end %>
  
  <%= turbo_frame_tag Quote.new %>
  <%= render @quotes %>

</main>